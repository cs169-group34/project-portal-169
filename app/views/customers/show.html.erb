<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @customer.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @customer.email %>
</p>

<table>
  <h3>projects</h3>
    <% @projects.each do |project| %>
      <tr>
        <%= link_to project.title, "/projects/#{project.id}" %>
      </tr>
    <% end %>
</table>

<%= link_to 'Edit', edit_customer_path(@customer) %> |
<%= link_to 'Back', customers_path %>
